# Runtime
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Tests. The engine itself needs none of the above: it is plain Python, which
# is why api/tests/ runs without installing anything.
pytest==8.3.4
